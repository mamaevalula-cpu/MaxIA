# ============================================================
# my_personal_ai — Dependencies
# ============================================================

# ── Обязательные ─────────────────────────────────────────────
httpx>=0.27.0               # HTTP-клиент (LLM API calls)
python-dotenv>=1.0.1        # .env поддержка

# ── GUI ───────────────────────────────────────────────────────
customtkinter>=5.2.2        # Современный UI (опционально, падает на tk)

# ── Шифрование ───────────────────────────────────────────────
cryptography>=42.0.0        # Fernet шифрование секретов

# ── Telegram ──────────────────────────────────────────────────
python-telegram-bot>=21.3   # Telegram-бот

# ── Векторные базы (опционально, ставь что нужно) ────────────
# chromadb>=0.5.0           # Локальный векторный store
# faiss-cpu>=1.8.0          # FAISS быстрый поиск
# sentence-transformers>=3.0 # Семантические эмбеддинги
# pinecone-client>=3.0.0    # Облачный Pinecone
# qdrant-client>=1.9.0      # Qdrant векторная БД

# ── Браузерная авторизация (опционально) ─────────────────────
# playwright>=1.44.0        # Браузер-автоматизация (рекомендуется)
# selenium>=4.21.0          # Альтернатива
# webdriver-manager>=4.0.1  # Авто-загрузка драйверов

# ── Торговля (для TradingAgent) ───────────────────────────────
# pybit==5.10.1             # Bybit V5 API
# pandas>=2.2.0
# numpy>=1.26.0

# ── Новые AI-инструменты (2025) ───────────────────────────────
duckduckgo-search>=6.3.0    # SearchAgent — бесплатный веб-поиск
feedparser>=6.0.11          # NewsAgent — RSS-ленты
beautifulsoup4>=4.12.0      # SearchAgent/SummarizerAgent — парсинг HTML
psutil>=5.9.0               # MonitorAgent — системные метрики
schedule>=1.2.0             # Планировщик фоновых задач

# ── Изображения (опционально) ─────────────────────────────────
Pillow>=10.0.0              # ImageAgent — базовая работа с изображениями
# pypdf>=4.0.0              # SummarizerAgent — чтение PDF
# pdfplumber>=0.11.0        # Альтернативный PDF-парсер

# ── Математика и финансы (MathAgent) ─────────────────────────
numpy>=1.26.0               # Числовые вычисления (VaR, Sharpe, Markowitz)
scipy>=1.11.0               # Статистика, IRR (brentq), вероятности
sympy>=1.12                 # CAS — символьная математика (уравнения, интегралы)
# PyPortfolioOpt>=1.5.5     # Продвинутая оптимизация портфеля (опционально)

# ── Разработка ────────────────────────────────────────────────
# pytest>=8.2.0
# ruff>=0.5.0
# black>=24.4.0
